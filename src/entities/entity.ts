import { Evidence } from 'app/generated/prisma';

export { Evidence };
// export interface Entity {
//   create(data);
//   //   delete<T>(data: Partial<T>): Promise<T>;
// }

// // EvidenceAdapter;. /
